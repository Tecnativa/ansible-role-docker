- hosts: test-playground
  roles:
  - role: ../..
    vars:
      docker_role_custom:
        app: &install {present: false}
        bootloader: false
        compose: *install
        engine: *install
        machine: *install
